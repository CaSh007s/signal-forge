import AgentClient from "./AgentClient";
export const dynamic = "force-dynamic";

export const metadata = {
  title: "Agent Console | SignalForge",
};

export default function AgentPage() {
  return <AgentClient />;
}
