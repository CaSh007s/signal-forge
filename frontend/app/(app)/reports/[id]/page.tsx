import ReportClient from "./ReportClient";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Report Viewer | SignalForge",
};

export default function ReportPage() {
  return <ReportClient />;
}
